# DivZero 开发版源码快照

版本标识：`0.1.0-SNAPSHOT`。这是开发候选源码，不是完整 V1，也不表示已经创建 GitHub Release。

## 支持环境与构建

- Minecraft **26.1.2**、NeoForge **26.1.2.106**、Java **25**。
- 源码沿用技术包名 `dev.mineagent.runtime` 与 Mod ID `mineagent_runtime`，以保持兼容。
- Gradle Wrapper **9.2.1**；Maven/Modrinth 依赖版本与校验值在构建文件中固定。
- 设置 `JAVA_HOME` 为 Java 25，然后在仓库根目录运行 `./gradlew.bat :neoforge:jar`（Windows）；其它平台使用 `./gradlew`。
- Windows 的可选媒体二进制不进入 Git。完整媒体版先通过 PowerShell 7 执行 `./scripts/prepare-media-tools.ps1`，它从固定上游下载并核对 SHA-256；未准备时不应宣称外部媒体功能可用。
- 输出位于 `neoforge/build/libs/`。源码 ZIP 不是可安装的 Mod；游戏内 WebGUI 还需要对应版本的 WebGUI/MCEF 依赖。
- `./gradlew.bat ci` 只构建该快照并运行随快照提供的离线测试，**不是完整 V1 验收**。历史维护/Native smoke 任务可能依赖未公开的测试存档或单独下载的游戏依赖，不作为公共安装入口。

## 当前可用流程

- API Key 保存后异步获取真实模型目录（标准 `/v1/models`）；Native 自动打开可点击列表，F2 API 设置可下拉选择、搜索/分页、刷新与保存；选择“使用自定义模型”才显示名称输入框，Native 也保留自定义模型输入。只请求模型元数据，不生成内容；接口错误保留当前模型，Key 不进入网页。

- 原生 `@AI名字` 与 F2 对话；模型可以通过工具读取玩家、物品、世界和已加载方块，再依据真实回执执行操作。
- 当前权限下直接修改已有物品的名称/附魔/组件，给予已注册物品，执行 Minecraft 指令与 GameRule。
- 普通聊天可以生成独立 RuntimePackage 源码、模型与行为，当前HOT内容发布后自动启用；其它生命周期仍使用对应入口。通用 HOT 物品载体支持包内彩色几何和真实 item.use/restyle：DeepSeek 生成的钥匙原样产物已验证给予背包、右键修改同一栈模型/名称、数量保持 1、停用拒绝。源码生成与批准后 Native 验收分阶段进行，没有用固定示例替代；载体不是热注册新的 FML Registry ID。
- 本人真实交互距离内的方块使用、原生菜单读取、整叠 QUICK_MOVE 和关闭。真实 DeepSeek 已完成只搬运箱中钻石、保留铁锭、关闭原生菜单并使用拉杆；实际客户端菜单、物品数量和陈旧请求拒绝均已核对。由模型读取/选择槽位，不要求玩家手输内部 ID；精确搬运 N 个及任意 Mod 特殊菜单尚未验收。
- 生成包可提供蓄力物品：服务端计时的 item.release/throwItem 将手中原件转为真实飞行实体，固体 AABB 碰撞反弹后可由原投掷者碰撞拾回；满背包保留，停用取消有效蓄力。真实 DeepSeek 近似球经历几何修正后，短/长蓄力、数量守恒及停止通过 Native；后续已补真实生成的球架/入框计分闭环；仍不覆盖任意 Mod 的 ItemEntity 专用拾取事件。
- AI 可发现并验证建模能力，使用 v2 参数化 sphere/torus 与平滑法线生成高精度几何，原 boxes/自由网格仍保留。真实生成的64×32主球最大三角面内缩约半径0.2405%，原生全分辨率3D预览和投掷回归通过。球面不再依靠堆盒子；物理仍是独立AABB；v2新增静态collisionMode=none视觉，配合独立solid边沿形成真正可穿过的空心通道。客户端和服务端应一同更新到本测试包。
- DeepSeek `deepseek-flash` 使用高强度 Thinking，工具续轮保留所需传输信息；Provider 返回的思考单独保存，原生仅显示 `[AI名字][思考]…`（无独立“思考中”提示），F2 历史默认折叠、点击展开分页查看。思考不混入正文/TTS/摘要，也不改变身体 HUD 的公开行动摘要。不注入人工输出 token 上限；Provider 自身边界与应用传输/操作次数保护仍存在。
- 方块/流体延迟观察；真实模型自主建造与修正刷石机后，独立测试连续三次移除产物均由流体重新生成。
- 单人本机所有者可以发现和读取当前实例 `schematics` 目录的 Create 导出 NBT。当前只读结构和材料，不执行或放置 NBT，不读取详细方块实体参数。
- 联网工具提供 MC 百科站内搜索与公开 HTTPS 正文读取，回答需引用来源；不执行网页脚本、不携带游戏登录 Cookie 或模型 API Key。通用 DuckDuckGo HTML 后端在部分网络会返回验证码/202，遇到此情况明确失败，不伪造搜索结果。

本次思考展示使用真实DeepSeek返回的39,943字符验证Native分段与持久原文一致；F2再以该原始结果做零模型历史复验，默认折叠、完整分页与SHA-256一致。简单请求未返回思考时不制造内容。

## 八个对话示例

篮球/篮球架/蓄力投篮/原件拾回/实际入框计分、手持剑直接附魔、给予新附魔剑、找钻石无结果扩大范围、暮色森林巫妖攻略检索、死亡不掉落与关闭PVP、创造/OP刷石机，均有真实 DeepSeek 与 Native 证据。篮球经历真实生成失败与修正，最终原样产物完成偏投0分、两次命中2→4分、多次反弹重入不重复计分，客户端原生sidebar与世界模型绘制确认。不是硬编码旧篮球样例。

最新找矿是模型自主8→16→24扩大，返回真实钻石矿坐标；该历史找矿证据只扫描已加载范围；当前坐标读取范围已扩展至4096³，按游标分段、不加载整个体积，不含自动采矿。攻略限定明确的Mod/Boss并带实际来源，不等于实战；刷石机已验证连续3次再生，不含生存资源获取。历史篮球验收先生成源码，再由玩家在F2包目录审查启用；当前新HOT生成改为自动启用；测试玩家瞄准/拾回不冒充AI自动寻路。

通用 `awardScoreOnce` 原子保存事件回执及总分后投影Native计分板，同事件不重复累加；每板最多512事件/32行，容量满拒绝而非遗忘去重，新游戏使用新实例。原生sidebar会替换当前侧栏。生成器会在发布前校验models目录中的RuntimeMesh格式；静态拼接面有1e-7格接触容差，不放开实质重叠。

这些示例闭环不是完整V1发布，以下其余缺口仍保留。

## 新增能力

- 参数化基础几何新增box、plane、disc、annulus、cylinder、cone、frustum、prism、pyramid、ellipsoid、capsule；连同sphere/torus共13种。支持三轴方向、平滑/硬边、封盖/双面等有界参数，仍共用4096顶点/8192面预算。真实DeepSeek通过发现工具、完整根示例及字段错误提示，提供并成功验证了11种新增形状；本轮是实际只读几何解析，不冒称逐种Native视觉验收。
- F2桌面“更多”和对话“更多”均可打开“API设置”：选择地址模板/保存API URL，Key通过原生掩码输入、加密保存/清除，不进入网页或模型上下文。改变有Key的地址须确认后续凭据使用范围；选择地址模板同时带入默认模型；自定义模型保留，不自动发起计费测试。
- 第8例历史后端已实际验证：DeepSeek自行发现已安装的网易云音乐，经本机确认后通过PowerShell打开，独立检查确认进程和可见窗口。没有内置软件路径或专用启动模板，没有下载安装。
- 这是通用 `inspect_host` / `python_execute` / `python_install_packages` 能力，不仅启动应用。最新改为Windows x64单人本机Owner的Java管理Python；每条脚本/第三方库安装需在原生聊天点击确认，不弹全屏窗口。不申请额外Windows权限，不是文件系统沙箱，不允许远程服务器借游戏OP操作客户端电脑。
- 最长60秒、stdout/stderr完整私有留存（即时回执各4096 UTF-8字节预览，read_host_output按游标续读全文），真实exitCode/PID/状态，超时/取消停止当前执行器及观察到的子进程，但已发生的效果可能保留。持久operation防重放；输出会返回当前模型，只应读取当前任务需要的信息。Java以UTF-8文件/参数数组直接调用专用Python，不经过PowerShell或Shell拼接；聊天可分页查看待确认源码。

## 配置与限制

API Key 只在本机游戏设置中填写，不放入聊天、代码、Issues 或截图。联网功能可由管理员在设置中开关。网页内容、蓝图、物品文字均为数据，不能增加世界/本机权限。

当前仍缺少：聊天修改已发布物品包及所有现存栈迁移、Create 及附属 Mod 运行兼容、任意 Mod 方块交互、完整种子地图、联网服务器的本机命令适配/非Windows平台等。语音识别按当前范围跳过；不将这些项目描述为已交付。

优先在独立测试实例中备份后安装，不同时放入多个同 ID 的 Mod JAR。来源中不包含任何用户存档、API Key、私人聊天记录或测试运行产物。

公开历史与发布方式见 [PUBLISHING.md](PUBLISHING.md)。依赖与资源说明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)；`.public-source-manifest.json` 列出本快照公开文件的校验值。

## 对话人设、外观、Buff与持续接管（2026-09-21）

- 可以直接说“以后你是简洁友善的星际向导”：读取当前AI的人设后按revision持久修改，同轮后续模型请求和未来会话使用最新版；不改变权限。人设与长期事实/偏好记忆是独立作用域。
- 可以说“把你的皮肤换成Alex”或“选个当前可用的YSM模型换上”：普通皮肤提供9人物×标准/纤细手臂共18款，也可复制聊天玩家的当前原生皮肤；不提供任意PNG/URL导入。YSM需要预先安装兼容版本和模型，切换即时生效、不需重启，卸载YSM本身才需重启。普通皮肤和YSM模式会互相清除旧覆盖。
- 可以说“给我60秒速度二级，隐藏粒子”“改成30秒一级”“只移除速度”：AI先读本人当前Buff和注册目录，再按hash写入/读回；正常倒计时不使hash失效；保留无关效果。写入使用本人真实游戏管理权限，不静默提权。
- 可以说“接管我，绕开障碍走到前面的金块，再看向石英块，等我下一条指令”：按最新设置，玩家明确要求即启动，不再弹首次或逐轮确认。左侧Native半透明HUD显示目标、公开行动摘要、下一路点及路线；不显示模型推理文本。
- 实际控制走原生键位和视角，不传送玩家；每项完成读取新观察，局部A*随新障碍/卡住重新寻路。真实DeepSeek测试中途中加入两格高石墙，玩家确实侧向绕行并到达金块、转向石英；仅一次批准、会话超过71秒仍保留。
- 达到目标进入IDLE待命而非退出，IDLE不持续请求模型。Esc或明确对话stop结束；普通游戏键鼠不结束；T/F2、失焦/界面暂时释放输入且会话保留。死亡/断线/世界或权限变化等异常保护仍停止；失败/未知保留待指令，不自动重试付费请求。
- 路径当前限已加载地面、单段水平16格/垂直8格、有界搜索，避开明显危险方块；不是全地图、飞行、游泳、载具或复杂容器自动操作。持续任务会产生后续模型调用，左侧HUD持续显示状态。电脑Python脚本/安装仍为逐条独立本机聊天确认，不包含在身体接管授权内。

本批23次真实DeepSeek请求用于人设/皮肤/Buff/接管/YSM。YSM原生应用成功后的测试记录曾因Optional序列化失败而中断，原失败保留；修复测试后仅重用原参数在新实例做零模型原生复验，确认实际模型渲染。未把Mock或纯字段回执当作真实模型/视觉验收。仍是测试包，不是完整V1发布。


## 2026-09-23 反馈更新

- DeepSeek默认`deepseek-flash`；新增GLM智谱/Z.AI地址模板，默认`glm-4.6`（兼容API接入，未使用GLM Key做真实计费验收）。空模型自动补全，切换已知默认模型随地址切换，自定义模型不静默覆盖。GLM端点参照[官方文档](https://docs.z.ai/guides/capabilities/mcp-call)。
- 原生聊天运行中提供“打断并发送这条消息”，处理中显示当前AI名字；支持AI设置聊天颜色及copy/suggest/confirm按钮，confirm作为新自然语言指令回到该AI，不直接执行任意脚本。真实DeepSeek已验证打断旧回复并完成新回复。
- AI自己的身体开放移动/寻路/转向/奔跑/选物品/丢物品/跟随/停止/传送到玩家；不是接管玩家。真实对话已验证传送、跟随、停止、选槽和丢一个物品；寻路为现有局部地面控制器。
- 玩家接管无二次弹窗，动态障碍绕行、转向、持续待命、聊天暂停恢复、Esc停止已用真实DeepSeek与Native验证；仅作用于提出请求的本人，死亡/断线/世界或权限变化仍保护释放。
- 移除旧4个AI创建上限；资源/区块票预算仍存在，并非无限硬件容量。Web管理16项分页；Native选择器每8个可前后翻页；原生名字按64条分包汇总，不再限制总数64。已Native实际创建70名、最后一页及返回、全部补全验证。
- `/ai accept`由实际OP/开启作弊的玩家完成游戏内初始化与信任，不提高OP权限、不代替电脑脚本审批；未开作弊进世界会提示。Key保存后返回原界面，保存按钮简化；Native重复Code Studio/媒体/计分板/创造器控件及专用界面已删除，Web高级管理仍保留。
- AI可自主持久读写事实/偏好记忆，按主题覆盖、TTL过期过滤，用户不需填写键；GUI记忆键自动生成。
- 方块范围支持最大4096×4096×4096坐标体积，用long游标逐页读取，不分配整个体积，不强制加载远端区块，未知/未加载不算扫描无结果。游戏命令全文输出保留10分钟并可分页继续；本机Python完整输出保存后可read_host_output分页，不丢弃超过即时预览的内容，不重跑脚本；仅当前本机Owner/当前server已执行operation可读。
- F2一级工具栏提供“批准 / 启用包”；当前HOT包各定义可直接启用，AI可查包与实际实例状态。历史HOT版本可“确认启用此版本”，校验后复制为独立包再启用，不回滚覆盖当前包或现存实例；Native实际按钮路径已验证。UNKNOWN/CONTEXT_CHANGED提供“核对后继续”，先读取持久回执和当前状态，不自动重放未知写操作。
- WebUI SDK开放`mineagentWorld.watch`，串行持续只读，隐藏暂停、关闭释放、失效停止；生成契约明确独立窗口与观察API。真实DeepSeek生成的读数台已在Native独立半透明窗口持续显示真实坐标与附近石头数，实际X 7.50→8.50、石头25→26匹配；刷新不调用模型。

本更新覆盖28条反馈对应实现，仍是测试版而非完整V1。真实验收不覆盖所有Provider/Mod/场景组合；GLM真实联验按用户选择暂缓；历史复制支持全部生命周期，BOOT/CLIENT仍走原有本机执行生命周期。ASR继续跳过。

本次新增真实验收：新生成的立体钥匙包无需F2审批自动ACTIVE并发放1件，原生右键改变同一栈的模型/名称，uses 0→1，停用后拒绝执行。实际画面确认紫色模型和正确AI名字；失败模型遗漏初始化的首份产物仍保留，不算通过。

## 28项续批：热生物与实时窗口

新增inspect_creatures/define_creature/control_creature；友好、中立、敌对物种使用统一持久Animal载体，自定义13种基础图形模型，可配置交易、附近丢物交换、靠近消息、主动攻击/受击反击、喂食繁殖、骑乘及跟随/停留/战斗/巡逻。空手普通右键骑乘，潜行或持有非食物物品右键交易，食物优先喂食。只有主人可下命令/骑乘，管理操作仍使用真实游戏权限；物种按世界/Owner隔离，修改需最新revision。

真实DeepSeek生成三类物种及星球几何，原始产物经Native验证七类行为、实际交易界面、骑乘移动、巡逻、伤害与NBT恢复。靠近事件支持消息、Buff、给物品、音效和可取消引信的原生爆炸，未提供任意脚本触发、外部纹理或骨骼动画，也不是动态注册新的FML实体ID。

本轮17次真实DeepSeek请求（生物6、网页11），high Thinking、不设人工max_tokens；原始失败完整保留，修复后仅对原样产物做0模型Native复验。网页实际读数、历史HOT按钮启用和70名Native目录通过。PowerShell完整输出/Unicode分页经实际PowerShell离线测试；不把离线测试当真实模型生成。

网络协议更新为4，客户端与服务端请同时更新。主要列表移除冗余r版本提示，技术详情的revision/hash继续用于准确核对。完整V1中Create运行适配、完整种子地图、任意Mod交互、已发布物品存量迁移等缺口仍保留。

## 28项收尾：失败恢复、生物事件与包启用

- 原生/F2失败回复可选“核对后继续”。F2仅最新失败可见，先刷新会话再发新请求，保留草稿并抑制双击；未知动作不重放。故障注入UNKNOWN与CONTEXT_CHANGED后，8次真实DeepSeek请求只读取持久操作记录和当前背包，回复完成；苹果始终1件、没有再次给予。故障前记录是明确的测试注入，不伪称模型曾执行。
- 生物进入范围可给予Buff、物品和播放声音；默认不破坏方块的爆炸带10..200tick引信，离开范围/死亡/骑乘/交易/更新定义会取消。5次真实DeepSeek请求生成礼物生物和爆炸生物，Native确认两次进入各给2苹果、速度二级、留圈不重复；引信取消，重新进入后真实爆炸伤害20→约2.08，地板保留。声音只验证原生调用，不声称独立音频监听验收。
- 目录当前/历史版本的“确认启用”：HOT直接启用；资源包下载验签后提交本机重载；数据包提交实际安装/重载；WORLD_REOPEN保存计划，须正常重开世界后生效。历史版本复制为独立包，不覆盖当前head/实例。BOOT/CLIENT原生代码保留每台机器独立的执行/安装流程，不能把目录批准解释为任意电脑脚本授权。
- 本批13次官方deepseek-flash，high Thinking、无人工max_tokens。GLM真实调用按用户选择暂缓；不以这次收尾宣称完整V1、所有Mod/Provider/行为组合均已验收。

追加Native已验证目录资源历史副本启用/停用、数据包实际重载与存档选择一致、世界重开计划保存/撤销（未自动重开）。修复普通目录DOM重绘误触发CONTEXT_CHANGED，以及NeoForge隐藏子包与顶层存档选择不一致导致的保存误拒；真实上下文/权限/hash和原生内容核对继续保留。

## Java管理Python与聊天确认（最新覆盖）

旧PowerShell执行器和全屏HostCommandScreen已删除；`host_command`不再进入模型工具目录。`python_execute`直接执行Python，`python_install_packages`调用专用venv的pip安装PyPI包（名称/版本约束/extras）；不依赖或修改系统Python、不需要activate脚本。下载/解压/venv/进程/超时/取消均由Java管理。

固定使用[python-build-standalone 20260901](https://github.com/astral-sh/python-build-standalone/releases/tag/20260901)的CPython3.13.15 Windows x64 `install_only_stripped`，归档21,936,514字节，SHA-256 `63d263ab0162f34a241a56dc5b283c22d6e131f5516117e6a921350c69ba7d4f`。归档及内容索引校验后解压到当前实例私有目录；不使用系统PATH发现Python。首次准备只在本机确认后发生，后续复用专用venv。其它OS/架构当前明确不可用，不回退系统解释器。

原生聊天提供“查看代码 / 确认执行 / 拒绝”，启动后有“停止”。本机随机短期令牌绑定精确请求/连接/本人，不把批准令牌交给模型或服务器；旧按钮、双击、断线失效。关闭聊天不等于停止已确认的进程；需要停止时点击按钮。Python依然以当前用户权限运行，并非文件系统沙箱。

脚本最长60秒，安装最长300秒；pip安装可能产生不可回滚的部分变化。完整stdout/stderr私有保存，首回执只预览、read_host_output分页；操作持久去重、失败/未知不自动重放。Java下载器支持已有HTTPS_PROXY/HTTP_PROXY非认证HTTP代理；凭据代理/其它平台不伪称已适配。API Key、运行时、第三方库、执行脚本/回执都不进入源码快照。

Python本批8次真实DeepSeek（含保留的下载失败），实际pip安装colorama、import版本读回、隐藏子进程及结果文件通过；最后对原样模型产物做0模型Native复验，聊天接受2次、拒绝1次、双击抑制2次、旧令牌拒绝1次，全程无全屏确认。失败与fixture修正均保留，不重放旧请求。

## 每个AI的语言模型

F2 → AI管理 → 目标AI的“模型”：默认使用全局设置，也可指定当前兼容API的模型；模型列表点击选择，只有“使用自定义模型”才显示名称输入。可随时保存恢复默认，不改其它AI或全局默认。复用现有API URL/Key，不是每AI一套独立Provider凭据。

配置按世界/AI UUID持久化、独立revision校验。对话/摘要/规划/代码生成等带Agent上下文的请求采用请求级Provider副本，不改共享默认；下次请求生效，已发请求不重发。API地址变化或指定模型不可用会明确失败，不默默回退。TTS/嵌入/独立图像服务仍用原有路由。

真实DeepSeek三次对话的requestedModel及responseModel均为flash→v4-pro→flash，全局默认始终flash，其他AI不受模型覆盖影响；另有0模型Native列表点击、自定义输入实际显隐与恢复默认验证。修复CEF冷模型目录轮询定时器receiver和该窗口hidden样式。这里只证明路由与已列出的实际场景，不宣称每个模型的所有工具组合都通过；GLM真实测试仍暂缓。

## 世界方块几何与累计循环上限

普通对话移除旧12轮/32次累计限制，保留主动停止、Provider/传输边界及未知操作不重放。AI可发现并使用通用方块几何：线、平面、墙/壳、非矩形拉伸、坡面、Bezier曲线、双线性曲面、圆柱/椭球/圆顶；镜像、Y轴90度旋转、阵列/路径重复；材料规则、完整BlockState和局部替换。

单计划包围盒最大2048×2048×2048，后台磁盘分页、分Tick实际操作，不按262144体素截断。必须满足实际游戏管理权限、世界高度/边界、已加载区块与磁盘空间；不改BlockEntity/NBT。计划与实际执行分开，有旧状态预检、一次消费和物理更新后回读，部分完成不自动回滚/重放。不等于完整WorldEdit/Axiom兼容或已填满2048³性能验收。

本批16次真实DeepSeek请求，在同一次原生聊天中完成15个工具轮次、61次工具调用和14份几何计划实际应用；最终209个非空气方块，物理读回匹配。另有0模型Native权限/陈旧/重复/取消/楼梯朝向/局部替换/箱子保护回归；离线磁盘计划264192格及2048各轴边界通过，未实际填满2048³。

## 联网建筑、文件交接与导入

AI可查询建筑站点/网页，获取公开建筑文件；遇反爬、登录或大文件不能直接读取时，通过聊天按钮打开F2“建筑文件”，用户选择上传并“交给AI”后自动继续。提供本地文件/世界文件夹上传、搜索分页与下载；Java实际传输仅127.0.0.1:25510，短期会话保留在本机，不开放公网或任意文件路径接口。单份上传8GiB、自动下载64MiB，当前单人Owner使用。

读取Litematica多region/负Size/位数组、Create或原版结构NBT、Sponge v1..3、旧原版数字ID schematic、Java Anvil世界选区及ZIP合集，转为真实世界几何计划。支持旋转/镜像及完整BlockState；源文件大于2048可选区，单计划仍≤2048每轴。当前blocks_only：不还原实体、BlockEntity payload、箱内物品、命令、计划tick和数据包；缺失方块明确失败，不换成空气，不覆盖既有BlockEntity，不运行上传世界。

26次真实DeepSeek验证搜索/读取、超大下载转手动提示、F2上传后的177块房屋实际落地。另有0模型Native上传/下载逐字节一致、90度旋转+镜像177/177状态匹配；修复弯角楼梯镜像后读回差异。9508份用户投影中9447完整解码、9466结构元数据通过，42份超大NBT仍受读取资源预算限制，尚未实现流式NBT；Java世界目录798地形region及一个实际chunk读取通过，不把这些算作完整世界复制或全部格式支持。


## 通用文件、预览和 PNG 热换肤（2026-09-24 最新覆盖）

F2 顶栏“文件”及对话“附件”共用通用文件库，支持文件/文件夹、空目录、空文件、二进制，文件夹下载 ZIP；AI 可 request_files / inspect_files / read_file / write_file / offer_file_download。只监听127.0.0.1:25510、Owner/世界认证，不是公网网盘。上传文件不自动执行，正文和网页不是指令。

导入支持 source_facing/target_facing 和旋转镜像，**默认保留内容**（含源空气、BlockEntity/容器、选区实体及Litematica/Anvil延迟Tick），可关闭。原始文件不变；旧 blocks_only 段落仅描述上一批。只在导入时调整朝向，不是已建建筑自动搬迁；复杂乘客/拴绳/悬挂和Mod跨坐标引用仍需专项适配，不安装源世界玩家/配置/数据包。42份超大NBT边界保留。

原生聊天思考开关可由F2对话更多或原生控制中心切换，只影响Native chat，F2历史不变。预览窗口支持旋转/平移/缩放、四向/俯视，显示RuntimeMesh物品/模型或建筑shape近似颜色；大建筑明确降为范围预览，不冒称完整材质渲染。

F2→AI管理→皮肤：本地PNG、内置/玩家皮肤热切换；AI可生成像素PNG、导出当前实际皮肤、修改并应用。64×64/旧64×32、wide/slim；更新GameProfile textures与观察者追踪，不删除服务端AI、保留UUID/名字/实例/跟随任务。网络协议5，客户端和服务端一起更新。

本批8次真实官方deepseek-flash请求（high Thinking、无人工max_tokens）：生成蓝白机器人→热换肤→导出修改金眼→再换肤、模型文件/下载/预览入口；同AI实例与跟随保留。初次客户端preview权限映射失败保留，修复后原参数新操作零模型Native通过；另验F2/Native思考开关、普通文件夹HTTP往返、PNG→Alex→PNG、南向建筑改北向且箱子7钻石/命名实体/延迟Tick保留。不是完整V1或任意格式/任意Mod保证；PNG/YSM所有组合与复杂NBT引用没有逐一验收。


## 2026-09-24 界面小修

F2入口及窗口标题统一为“包管理”。结构/模型预览取消预设视角下拉，改为鼠标连续拖动旋转、滚轮双向缩放；右/中键平移、复位保留，Native atlas 持续拖动使用实际Pointer Capture，关闭/取消会释放。NeoForge Mod 图标使用用户工作区的DivZero主图标（1024×1024 PNG），不修改Mod ID。该批只做零模型界面回归，不新增付费模型调用。
